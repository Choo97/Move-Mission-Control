spring:
  datasource:
    url: jdbc:h2:mem:moving-reservation-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: false
  mail:
    host:

notification:
  email:
    enabled: false
    from:

map:
  kakao:
    enabled: false
    rest-api-key:

upload:
  reservation-photo:
    directory: target/test-uploads/reservation-photos

app:
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://localhost:3000
