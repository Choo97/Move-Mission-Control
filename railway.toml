[build]
builder = "RAILPACK"
buildCommand = "mvn -DskipTests clean package"

[deploy]
startCommand = "java -jar target/moving-reservation-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
